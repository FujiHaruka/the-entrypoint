/**
 * Test for workers.
 * Runs with mocha.
 */
'use strict'

const workers = require('../lib/workers')
const { ok, equal } = require('assert')

describe('workers', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
